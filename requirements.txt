# Python 3.12. Pinned to the versions used for the P0 audit (2026-09-12).
# Add packages only when code needs them, and record why in docs/DECISIONS.md if they affect results.
numpy==2.4.4
PyYAML==6.0.3
pytest==9.0.2
# canonical interim dataset v1 (Parquet, typed columns; scripts/build_canonical_v1.py, D-028)
pyarrow==24.0.0
# optional: QA figures of scripts/audit_temporal_gaps.py (skipped if not installed; no effect on results)
# P3+ model training (D-043). Install the build matching your CUDA; P3 used 2.12.0+cu126.
torch==2.12.0
matplotlib==3.10.9
